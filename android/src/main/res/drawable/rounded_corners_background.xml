<!-- rounded_corners_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <corners android:radius="16dp" /> <!-- Adjust the radius to control the roundness of the corners -->
    <solid android:color="@android:color/white" /> <!-- Background color of the popup -->
    <stroke android:width="1dp"
            android:color="#919191" />
</shape>
